{
  "name": "tg-business-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf:d1": "wrangler d1 create tg-business-bot",
    "cf:kv": "wrangler kv:namespace create TG_KV",
    "cf:secrets": "wrangler secret put BOT_TOKEN && wrangler secret put ACCESS_PASSWORD && wrangler secret put WEBHOOK_SECRET"
  },
  "dependencies": {
    "croner": "^9.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.91.0"
  }
}
